{
  "name": "kipris-skill",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "KIPRIS 한국 지식재산 검색 스킬 (Claude Code / Claude Desktop)",
  "license": "MIT",
  "scripts": {
    "official-spec": "bun run tools/extract-official-spec.ts",
    "catalog": "bun run tools/build-catalog.ts",
    "test": "python3 -m unittest discover -s tests -t .",
    "package": "bash tools/package-skill.sh",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1",
    "prettier": "^3.9.6"
  }
}
